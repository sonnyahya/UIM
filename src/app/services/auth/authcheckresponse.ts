import { Claims } from './claims';

export class AuthCheckResponse {
    requestId: string;
    requestDate: string;
    responseId: string;
    responseDate: string;
    status: string;
    error: string;
    message: string;
    claims: Claims;
}
