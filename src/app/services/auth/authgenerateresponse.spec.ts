import { AuthGenerateResponse } from './authgenerateresponse';

describe('AuthGenerateResponse', () => {
  it('should create an instance', () => {
    expect(new AuthGenerateResponse()).toBeTruthy();
  });
});
