import { GetSellableProductResponse } from './getsellableproductresponse';

describe('GetSellableProductResponse', () => {
  it('should create an instance', () => {
    expect(new GetSellableProductResponse()).toBeTruthy();
  });
});
