import { GetOrderPackRequest } from './getorderpackrequest';

describe('GetOrderPackRequest', () => {
  it('should create an instance', () => {
    expect(new GetOrderPackRequest()).toBeTruthy();
  });
});
