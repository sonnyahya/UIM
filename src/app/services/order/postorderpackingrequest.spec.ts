import { PostOrderPackingRequest } from './postorderpackingrequest';

describe('PostOrderPackingRequest', () => {
  it('should create an instance', () => {
    expect(new PostOrderPackingRequest()).toBeTruthy();
  });
});
