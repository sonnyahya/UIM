import { PostUploadOrderRequest } from './postuploadorderrequest';

describe('PostUploadOrderRequest', () => {
  it('should create an instance', () => {
    expect(new PostUploadOrderRequest()).toBeTruthy();
  });
});
