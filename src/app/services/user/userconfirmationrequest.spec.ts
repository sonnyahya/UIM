import { UserConfirmationRequest } from './userconfirmationrequest';

describe('UserConfirmationRequest', () => {
  it('should create an instance', () => {
    expect(new UserConfirmationRequest()).toBeTruthy();
  });
});
