import { UserConfirmationResponse } from './userconfirmationresponse';

describe('UserConfirmationResponse', () => {
  it('should create an instance', () => {
    expect(new UserConfirmationResponse()).toBeTruthy();
  });
});
