import { UserNotifyResponse } from './usernotifyresponse';

describe('UserNotifyResponse', () => {
  it('should create an instance', () => {
    expect(new UserNotifyResponse()).toBeTruthy();
  });
});
