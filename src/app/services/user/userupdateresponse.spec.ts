import { UserUpdateResponse } from './userupdateresponse';

describe('UserUpdateResponse', () => {
  it('should create an instance', () => {
    expect(new UserUpdateResponse()).toBeTruthy();
  });
});
